<div class="mt-4">
    <div class="row">
        <div class="col-lg-4">
            <button>MUA NGAY</button>
        </div>

        <div class="col-lg-4">
            <h2>{{book.name}}</h2>
            <img class="card-img-top" src="{{book.image}}" alt="{{book.name}}">
        </div>


    </div>

</div>